---- .env ----
OPENCLAW_CONFIG_DIR: present — length=39 prefix=/hom suffix=nfig
OPENCLAW_WORKSPACE_DIR: present — length=42 prefix=/hom suffix=pace
OPENCLAW_GATEWAY_TOKEN: present — length=64 prefix=850b suffix=06e6
OPENCLAW_IMAGE: present — length=14 prefix=open suffix=ocal
GLM_API_KEY: present — length=49 prefix=0613 suffix=31Co
---- openclaw_config/.env ----
OPENAI_API_KEY: present — length=164 prefix=sk-p suffix=vLEA
GLM_API_KEY: present — length=49 prefix=0613 suffix=31Co
