# Sanitized Archive

This directory is for sanitized operational snapshots only.

Allowed examples:

- `openclaw.json`
- `paired.json`
- `pending.json`
- `gateway.log`
- `Caddyfile`

All secrets must be replaced by `<REDACTED>` before files are added.
